# Production environment configuration for Azure
NODE_ENV=production
PORT=8080

# Database Configuration - Use Azure SQL or SQLite
DB_FILE=/tmp/notes_v2.db

# JWT Configuration - Use strong secrets from Azure Key Vault
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRES_IN=24h

# Logging
LOG_LEVEL=info

# CORS Configuration
FRONTEND_URL=${FRONTEND_URL}

# Azure Application Insights (if enabled)
APPLICATIONINSIGHTS_CONNECTION_STRING=${APPLICATIONINSIGHTS_CONNECTION_STRING}
